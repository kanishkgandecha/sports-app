{
  "name": "sports-platform",
  "private": true,
  "version": "0.0.0",
  "engines": {
    "node": ">=22.6"
  },
  "packageManager": "pnpm@9.15.9",
  "pnpm": {
    "overrides": {
      "deepmerge-ts": "^8.0.0"
    }
  },
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "eslint . --max-warnings=0",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "test:unit": "turbo run test",
    "test:integration": "node scripts/require-database-url.mjs && turbo run test:integration --concurrency=1",
    "db:generate": "pnpm --filter @sports/db exec prisma generate",
    "db:migrate": "node --env-file=.env scripts/run-with-root-env.mjs pnpm --filter @sports/db exec prisma migrate dev",
    "db:migrate:deploy": "node --env-file=.env scripts/run-with-root-env.mjs pnpm --filter @sports/db exec prisma migrate deploy",
    "db:seed": "node --env-file=.env scripts/run-with-root-env.mjs pnpm --filter @sports/db exec tsx prisma/seed.ts",
    "archive:f1:rolling": "pnpm --filter @sports/ingestion history:f1:rolling && pnpm --filter @sports/ingestion history:f1:standings:rolling && pnpm --filter @sports/ingestion history:f1:details:rolling && pnpm --filter @sports/ingestion history:f1:analysis:rolling"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.5",
    "eslint": "^9.39.5",
    "eslint-plugin-jsx-a11y": "^6.10.2",
    "eslint-plugin-react-hooks": "^7.1.1",
    "globals": "^17.11.0",
    "prettier": "^3.9.6",
    "turbo": "^2.5.8",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.67.0"
  }
}
